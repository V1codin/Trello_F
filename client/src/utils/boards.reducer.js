//import { } from "./actions.types";

const init = {
  name: "",
  background: "",
  ownerId: "",
  memberIds: [],
};

function boards(state = init, { type, payload }) {
  switch (type) {
    default:
      return state;
  }
}

export { boards };
